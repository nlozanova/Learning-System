@using (Html.BeginForm("UploadFile", "Home", FormMethod.Post, new { enctype = "enctype='multipart / form - data" }))
{

    <input type="file" name="myFile" />
    <input type="submit" value="Send" />
}
